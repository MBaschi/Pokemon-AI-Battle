# Dipendenze per i notebook di meta-analysis
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
beautifulsoup4>=4.12
lxml>=4.9
requests>=2.31
truststore>=0.9      # usa il trust store dell'OS (necessario dietro proxy aziendali)
certifi>=2024.0      # fallback per la verifica TLS
nbformat>=5.9
nbconvert>=7.0
ipykernel>=6.29
kaggle>=1.6          # opzionale: solo per la leaderboard Kaggle (Sezione 7 del notebook 1)
