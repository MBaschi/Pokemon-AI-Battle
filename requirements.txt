# Dipendenze Python per la simulazione locale e l'esport dei replay.
#
# L'engine della competizione (il package `cg/` e il suo .so) NON e' un package pip:
# va scaricato dalla pagina Kaggle della competizione e il package `cg/` va messo
# nella root del repository. Vedi README.

# --- Core: simulazione partite / ladder ---
# Bloccato alla versione che gira sulla ladder Elo. Non aggiornare con leggerezza.
#kaggle-environments==1.30.1

# --- Analisi dati e notebook (meta_analysis/, view_replays/) ---
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# --- Scraping meta e tornei reali (meta_analysis/) ---
requests>=2.31
beautifulsoup4>=4.12
certifi>=2024.2.2   # bundle CA usato dallo scraper per il TLS
truststore>=0.8     # opzionale: usa il trust store di sistema quando disponibile

# --- Integrazione Kaggle (view_replays/, download submission e replay) ---
kaggle>=1.6

# --- State evaluator: dataset, classificatore, modello (agents/state_evaluator/) ---
scikit-learn>=1.3
torch>=2.0          # richiesto solo per l'encoding/training basato su rete neurale
